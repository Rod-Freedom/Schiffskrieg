<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Schiffskrieg</title>
</head>

<body class="w-screen font-serif text-white bg-blue-700 flex flex-col justify-center align-middle">

  <header class="flex flex-col justify-center p-6">
    <h1 class="text-9xl"> 
      <a href="./">Schiffskrieg</a>
    </h1>
    {{#if logged_in}}
    <nav>
      <ul>
        <li><button class="text-white bg-gradient-to-r from-blue-400 via-blue-500 to-blue-600 hover:bg-gradient-to-br focus:ring-4 focus:outline-none focus:ring-blue-300 dark:focus:ring-blue-800 font-medium rounded-lg text-3xl px-5 py-2.5 text-center me-2 mb-2" id="logout">logout</button></li>
        {{!-- TODO: add player name instead of profile --}}
        <li><button class="text-white bg-gradient-to-r from-blue-400 via-blue-500 to-blue-600 hover:bg-gradient-to-br focus:ring-4 focus:outline-none focus:ring-blue-300 dark:focus:ring-blue-800 font-medium rounded-lg text-3xl px-5 py-2.5 text-center me-2 mb-2" id="profile">Profile</button></li>
      </ul>
    </nav>
    {{/if}}
  </header>


  <main>
    {{{ body }}}
  </main>
  <footer>
      
  </footer>

  {{#if logged_in}}
  <script src="/js/main.js"></script>
  {{/if}}
</body>

</html>